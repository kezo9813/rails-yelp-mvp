  
<div class="container">
  <h1>All the Restaurants</h1>
  <ul><% @restaurants.each do |restaurant| %>
   <li><%= link_to restaurant.name, restaurant_path(restaurant) %></li>
   <% end %>
  </ul>
  <%= link_to "Add a restaurant", new_restaurant_path, class: 'btn' %>
</div>